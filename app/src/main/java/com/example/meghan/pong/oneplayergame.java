package com.example.meghan.pong;

import android.graphics.Bitmap;
import android.graphics.Canvas;
import android.graphics.Paint;
import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.SurfaceHolder;
import android.view.SurfaceView;

import java.util.Timer;

/**
 * Created by Meghan on 11/29/2016.
 */
/*
public class oneplayergame extends AppCompatActivity { //DO i need extends AppCompatActivity

    //Making the size of the screen for the game
    //int screenWidth = 300; //options for width and height to make screen not square
    //int screenHeight = 400;
    static int screenSize = 300; //Square screen



    //bars, used to defend the ball from going past your side
    static int bar_defender = 12;

    //for ball size
    //static int ballX = 3;
    //static int ballY = 3;
    static int ballSize = 8;

    //how fast the ball moves
    static double ballSpeed = 5;

    static double ballDirection = Math.PI + 1; //heads up and left

    static Timer timer;

    static GameDisplay gamePanel;

    private static class GameDisplay   {   //extends JPanel

    }

    public static void main(String[] args) {

        gamePanel = new GameDisplay();

    }



    //GameView gameView;
    //Do I need this rubber ducky

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        //gameView = new GameView(this);
        setContentView(R.layout.solo_player_layout);
        //setContentView(gameView);

    }


    //Simple game engine here?



}
*/
